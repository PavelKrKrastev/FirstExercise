<UserControl x:Class="NemetschekFirstAssigment.View.Advanced1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NemetschekFirstAssigment.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel VerticalAlignment="Center">
            <Label Content="Choose language:"/>
            <ComboBox ItemsSource="{Binding LanguageMode}" SelectedIndex="0"></ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <Label Content="Select a power mode:"/>
            <ComboBox ItemsSource="{Binding PowerMode}" SelectedIndex="0"></ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="2" VerticalAlignment="Center">
            <Label Content="Select battery mode:"/>
                <ComboBox ItemsSource="{Binding BatteryMode}" SelectedIndex="0"></ComboBox>
        </StackPanel>

    </Grid>
</UserControl>
